#pragma once

namespace wire {
    // A class that owns the states of all existing nodes in the whole game
    
    class WireNet {
    public:
        WireNet() noexcept;
        ~WireNet() noexcept;

        

    private:

    };
}