#pragma once

namespace wire {
    // top-level class which keeps track of all game subsystems and handles player connections
    
    class WireServer {
    public:
        WireServer() noexcept;
        ~WireServer() noexcept;

        

    };
}